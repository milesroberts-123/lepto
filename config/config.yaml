# --- KMC ---
k: 31
mincount: 1
maxcount: auto

# --- fastp ---
unqualLimit: 40
qualThresh: 20
windowLength: 4
k: 31
nBaseLimit: 5

# --- kmc_filter: how to build group-specific k-mer set ---
subtract_groups: true
min_group_count: 0
filter_min_kmer_support: 1

# --- metaSPAdes ---
metaspades:
  kmers: "auto"
  tmp_dir: "/tmp"

# --- kmc_tools complex params ---
kmc_intersect_depth: 100

# --- samples ---
samples_table: "../config/samples.tsv"

# --- general ---
conda_prefix: "envs"
